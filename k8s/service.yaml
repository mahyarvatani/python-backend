apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: python-backend
spec:
  type: NodePort
  selector:
    app: backend
  ports:
  - name: http
    port: 80
    targetPort: 8888
    nodePort: 30080

